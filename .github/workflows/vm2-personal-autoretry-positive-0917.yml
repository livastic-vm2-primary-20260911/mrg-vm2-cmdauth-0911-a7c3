name: VM2 Personal Auto Retry Positive 0917 A9F4
on:
  workflow_dispatch:
jobs:
  vm2-personal-autoretry-a9f4:
    runs-on: ubuntu-latest
    steps:
      - name: Controlled fail-on-first-attempt
        shell: bash
        run: |
          echo "attempt=${GITHUB_RUN_ATTEMPT}"
          if [ "${GITHUB_RUN_ATTEMPT}" = "1" ]; then
            exit 1
          fi
          exit 0
