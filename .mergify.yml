commands_restrictions:
  queue:
    conditions:
      - sender-permission >= write
  dequeue:
    conditions:
      - sender-permission >= write
queue_rules:
  - name: vm2-ff-review-a9f4
    merge_method: fast-forward
    queue_conditions: []
    merge_conditions:
      - check-success = vm2-ff-hold-never-a9f4
