merge_protections:
  - name: VM2 local invalid-config stale check A9F4
    success_conditions:
      - title = VM2_LOCAL_INVALID_CONFIG_A9F4
queue_rules:
  - name: default
    merge_conditions:
      - check-success = vm2-never-pass
